bank-report-rag/
├─ README.md                 # 项目说明（卖点、架构图、运行步骤）
├─ requirements.txt          # 依赖
├─ .gitignore
│
├─ data/
│  ├─ raw/
│  │  ├─ pdf/               # 原始年报 PDF
│  │  └─ html/              # 原始 HTML（如果有）
│  ├─ interim/
│  │  └─ cleaned_text/      # 粗清洗后的 txt（按文档存）
│  └─ processed/
│     ├─ chunks/            # 分chunk后的文本（jsonl / parquet）
│     ├─ embeddings/        # 向量（可选，numpy / .pt）
│     └─ index/             # Faiss 索引文件
│
├─ configs/
│  ├─ paths.yaml             # 各种路径和文件名
│  └─ rag.yaml               # chunk 长度、top_k 等超参
│
├─ src/
│  ├─ __init__.py
│  ├─ config.py              # 读取 configs，封装成 Python 对象
│  │
│  ├─ ingestion/             # 数据获取 & 解析
│  │  ├─ pdf_to_text.py      # PDF → 结构化 text
│  │  └─ html_to_text.py     # （可选）HTML → text
│  │
│  ├─ preprocessing/
│  │  ├─ clean_text.py       # 去页眉页脚、噪声、空行等
│  │  └─ structure_detect.py # 识别章节、标题（简单规则）
│  │
│  ├─ chunking/
│  │  └─ chunker.py          # 章节 + 滑窗 chunk
│  │
│  ├─ embedding/
│  │  └─ build_embeddings.py # bge-m3 生成 embedding
│  │
│  ├─ index/
│  │  ├─ build_faiss.py      # 构建 Faiss IVF-PQ 索引
│  │  └─ search_faiss.py     # 检索接口（给 RAG 用）
│  │
│  ├─ rag/
│  │  ├─ retriever.py        # 召回 + 重排（可选）
│  │  ├─ prompt_templates.py # 提示模板
│  │  └─ qa_pipeline.py      # end-to-end RAG pipeline
│  │
│  └─ ui/
│     ├─ cli.py              # 命令行问答（最简）
│     └─ web_app.py          #（可选）Gradio / Streamlit demo
│
└─ notebooks/
   ├─ 01_explore_corpus.ipynb      # 语料探索
   ├─ 02_chunking_sanity_check.ipynb
   └─ 03_rag_eval.ipynb            # 简单评测 & case study
